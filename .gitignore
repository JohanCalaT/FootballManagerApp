# --- General ---
CLAUDE.md
api-football-sdd.md
.claude/
.gemini/
.idea/
.vscode/
.vs/
*.swp
*.bak
*.tmp
*.log

# --- Secrets ---
# Firebase Admin SDK service account JSONs — NEVER commit.
# Real keys live in dotnet user-secrets (local) / Key Vault (cloud).
*-firebase-adminsdk-*.json
firebase-adminsdk*.json
serviceAccount*.json

# --- Local-only Firebase / MCP tooling config ---
# Source of truth is the Firebase console + each dev's local CLI auth.
.firebaserc
firebase.json
.mcp.json

# --- Frontend runtime config ---
# Generated by frontend/scripts/write-config.js from FIREBASE_* env vars at
# `npm start` / `npm run build`. Never commit — regenerate per environment.
frontend/src/assets/config.json

# --- Cypress / Firebase Emulator runtime artifacts ---
frontend/cypress/screenshots/
frontend/cypress/videos/
frontend/cypress/downloads/
firebase-debug.log
firestore-debug.log
ui-debug.log
firebase-export-*/

# --- Stencil build artifacts ---
# Source lives in frontend/stencil/src/. Everything below is generated by
# `stencil build` / `npm install` and reproduced from source.
frontend/stencil/dist/
frontend/stencil/www/
frontend/stencil/loader/
frontend/stencil/.stencil/

# --- Node.js / Angular / Ionic ---
node_modules/
dist/
build/
www/
.angular/
.cache/
.npm/
.parcel-cache/
.pnp*
.yarn/
coverage/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- .NET / C# ---
bin/
obj/
TestResults/
*.user
*.userosscache
*.sln.docstates
*.suo
*.userprefs
.nx/

# --- OS Specific ---
.DS_Store
Thumbs.db
.azure

# --- Local-only design docs (SDDs) ---
# Notas de diseño que viven en la máquina del autor. No se publican al repo.
docs/
DESIGN.md

# --- Stitch design exports (referencia local, no se versionan) ---
stitch-export/

# --- Design assets droppeados en la raíz para drag&drop, no se versionan ---
# El asset real vive en frontend/src/assets/.
/*.svg
/*.png
/*.jpg
/*.jpeg

# --- Firebase service account (si se descarga, nunca al repo) ---
**/firebase-adminsdk*.json
**/serviceAccountKey.json
**/google-services.json

# --- Capacitor / Android / iOS (generados por Ionic build) ---
android/
ios/
