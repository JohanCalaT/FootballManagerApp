// Public surface — re-export each component so consumers can import types.
// Components register themselves as custom elements when this module loads
// in a host that calls defineCustomElements (Angular's main.ts).
export { FmaPlayerCard } from './components/fma-player-card/fma-player-card';
